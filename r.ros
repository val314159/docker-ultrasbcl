#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.r.3827495943
  (:use :cl))
(in-package :ros.script.r.3827495943)

(load "r.lisp")

(defun main (&rest argv)
  (declare (ignorable argv))
  (r:cli argv))

;;; vim: set ft=lisp lisp:
